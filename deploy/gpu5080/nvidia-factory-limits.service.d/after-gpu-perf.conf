# Factory PL after gpu-perf drop-in and after 1080 Ti vfio-bind.
# nvidia-smi then sees only the host RTX 5080. Do not unmask Comfy.
[Unit]
After=akula-gpu-perf.service nvidia-persistenced.service vfio-bind-1080ti.service
