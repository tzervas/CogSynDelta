main_rule best_primary_metric_all_gates_pass on train:$.held_out.recall@1, applied within the same code pin (7bc2699), the same corpus fingerprint (af4bb36f6) and the production batch (1280), among the retrieve cells whose train, test, quantize and test-quant stages all passed. The seed axis resamples the held-out split, and here it matters: seed 1 scored 0.8535 and seed 0 scored 0.7559 on different splits (both untrained baselines 0.0020), so the two numbers are not comparable and the gap is not evidence that seed 1 is a better model. Seed 0 is the canonical split (the matrix's own baseline cites it) and is promoted: retrieve-b1280-s0 (0.7559). Receipts are csd-metrics/v1. Promoted variant: retrieve-b1280-s0-7bc2699-20260904-caf4bb36f.
