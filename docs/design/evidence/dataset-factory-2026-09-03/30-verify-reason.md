# REASON faculty — adversarial licence verification

Verifier pass, 2026-09-03, against `10-survey-reason.md`. Method: WebFetch/WebSearch against
the primary source named (or the correct primary source, where the surveyor's was wrong) for
every candidate with a verdict other than REFUSE, plus the REFUSED entries. No dataset content
downloaded — cards, README raw text, API JSON, and licence files only. Every fetch below is
independently reproducible from the URL given.

Legend: **VERIFIED-AGREES** / **CONTRADICTED** (corrected verdict + quoted clause) /
**UNVERIFIABLE** (what was missing).

---

## Top 8

### 1. `EleutherAI/hendrycks_math` (MATH) — VERIFIED-AGREES
- Fetched `raw.githubusercontent.com/hendrycks/math/main/LICENSE` myself: full MIT License
  text, "Copyright (c) 2021 Dan Hendrycks" — matches the surveyor's quote verbatim.
- Independently confirmed the HF mirror's `cardData.license` = `"mit"` — matches.
- Verdict PERMISSIVE_OK: agrees.

### 2. `openai/prm800k` — VERIFIED-AGREES
- Fetched `raw.githubusercontent.com/openai/prm800k/main/LICENSE` myself: full MIT License
  text, "Copyright (c) 2023 OpenAI" — matches the surveyor's quote verbatim.
- Note: `openai/prm800k` is not itself a resolvable HF dataset repo (`api/datasets/openai/prm800k`
  returns HTTP 401 `"Invalid username or password."`, HF's generic response for a path that
  isn't a real public repo) — the surveyor's own row correctly treats `openai/prm800k` as the
  **GitHub** id and lists the HF mirrors separately (`tasksource/PRM800K`,
  `Birchlabs/openai-prm800k-*`). Checked `tasksource/PRM800K`: `cardData.license` = `"mit"` —
  matches the surveyor's "mirror_tag: mit (matches)" claim.
- Verdict PERMISSIVE_OK: agrees.

### 3. `open-web-math/open-web-math` — **CONTRADICTED** (evidentiary claim only; verdict itself agrees)
- README prose confirmed verbatim: *"OpenWebMath is made available under an ODC-By 1.0 license;
  users should also abide by the CommonCrawl ToU."* Verdict ATTRIBUTION (ODC-By) is correct.
- **But** the surveyor's specific claim — *"mirror_tag: none set on `cardData.license` (blank
  on the API — mismatch... exactly the kind of mirror-lies gap"* — is factually wrong. I fetched
  both the raw README YAML frontmatter (`license: odc-by`) and the live API
  (`huggingface.co/api/datasets/open-web-math/open-web-math`), and `cardData.license` is
  explicitly `"odc-by"`, not blank. There is no mirror/prose mismatch here; mirror and prose
  agree. Corrected finding: **no mirror-lies gap on this entry** — drop the flag, keep the
  ATTRIBUTION verdict (which rests on the prose+tag agreeing, not on a gap).
- `grant_scope` nuance (ODC-By licenses the compilation, not each crawled page's underlying
  copyright) is the surveyor's own legal reasoning, not an externally-checkable fact; it's a
  standard, defensible reading of ODC-By over a CC-derived corpus and I found nothing that
  contradicts it. Left as INFERRED-but-reasonable, unchanged.

### 4. `camel-ai/math` — VERIFIED-AGREES
- HF card confirmed: license tag `cc-by-nc-4.0`; prose confirmed verbatim: *"This data was
  synthetically generated by GPT4 and might contain incorrect information. The dataset is there
  only for research purposes."*
- Verdict NC (not REFUSE, per DEC-31): agrees — the "research purposes" sentence is card prose,
  not a separate legal term overriding the stated CC BY-NC-4.0 grant; no ND/no-redistribution
  clause found anywhere fetched.

### 5. `hoskinson-center/proofnet` — VERIFIED-AGREES
- Fetched `raw.githubusercontent.com/zhangir-azerbayev/ProofNet/main/LICENSE` myself: full MIT
  License text, "Copyright (c) 2022 zhangir-azerbayev" — matches (surveyor didn't quote a date;
  content otherwise matches verbatim).
- Verdict PERMISSIVE_OK: agrees.

### 6. `allenai/math_qa` (MathQA) — VERIFIED-AGREES
- HF card confirmed: license tag `apache-2.0`; prose: *"The dataset is licensed under the
  Apache License, Version 2.0."*
- AQuA-RAT provenance claim confirmed verbatim from the README: *"Our dataset is gathered by
  using a new representation language to annotate over the AQuA-RAT dataset with fully-specified
  operational programs"* / *"AQuA-RAT has provided the questions, options, rationale, and the
  correct options."* — the `aqua-rat` provenance-group placement (no B1/B2 gain) is correct.
- Verdict PERMISSIVE_OK-but-same-group: agrees.

### 7. `open-r1/OpenR1-Math-220k` — VERIFIED-AGREES
- HF card confirmed: license tag `apache-2.0`.
- Construction claim confirmed verbatim: *"we prompt DeepSeek R1 model to generate solutions for
  400k problems from NuminaMath 1.5"*; verification method confirmed (*"Math Verify for most
  samples and Llama-3.3-70B-Instruct as a judge for 12% of the samples"*); 400k→220k
  (94k default + 131k extended) filtering-to-verified-correct claim confirmed.
- Verdict PERMISSIVE_OK, `numina-aops` provenance-group placement: agrees.

### 8. `wics/strategy-qa` — VERIFIED-AGREES
- Confirmed `cardData.license` on the `wics/strategy-qa` HF card = `"other"` — matches the
  surveyor's claimed mismatch.
- Fetched `github.com/eladsegal/strategyqa/blob/main/LICENSE` myself: full MIT License text,
  "Copyright (c) 2021 Elad Segal" — matches the surveyor's "upstream says MIT" claim verbatim.
- Verdict PERMISSIVE_OK (upstream overrides the vaguer mirror tag): agrees. This is a real,
  correctly-caught instance of the mirror-lies pattern (unlike #3 above, which was a false
  positive).

---

## Notable non-top-8 entries

### `AI-MO/NuminaMath-CoT` — VERIFIED-AGREES
- License Apache-2.0 confirmed on the HF card.
- `orca_math` subset = `microsoft/orca-math-word-problems-200k` confirmed independently (web
  search corroborates: regex-based answer extraction/simplification applied when folding
  Orca-Math into NuminaMath-CoT; same GPT4-Turbo-generated source).
- `aops_forum` subset row count (30,201) matches what I saw in the card's source-breakdown
  table; I found nothing this session establishing the individual AoPS forum posters' licence
  chain either — the surveyor's `provenance_red_flags` note stands unresolved, correctly held
  short of REFUSE (Apache-2.0 is a real grant from AI-MO, the distributor) and short of a clean
  admit (NC/PERMISSIVE_OK-with-caveat). Agrees.

### `meta-math/MetaMathQA` — VERIFIED-AGREES on substance; **CONTRADICTED** on sourcing detail
- Mirror tag `mit` confirmed directly from the raw README YAML frontmatter
  (`license: mit`).
- The GPT-3.5 rephrasing fact itself is TRUE: confirmed via the MetaMath paper (arXiv:2309.12284,
  ICLR 2024) and corroborating secondary coverage — GPT-3.5 was used to rephrase questions
  (`GSM_Rephrased`/`MATH_Rephrased` splits), with few-shot CoT used to check rephrased-question
  correctness.
- **But** I could not find that sentence, or any ChatGPT/GPT-3.5/GPT-4 mention, anywhere in the
  actual `meta-math/MetaMathQA` HF card text (checked both the rendered card and the raw
  README.md) — only "All MetaMathQA data are augmented from the training sets of GSM8K and
  MATH," with no method stated. The surveyor's specific attribution — *"HF card prose states
  augmentation is 'sourced from ChatGPT 3.5'"* — is **CONTRADICTED as to source**: that fact
  comes from the paper/GitHub repo, not the HF card prose. The underlying fact and the resulting
  NC-hold verdict are unaffected; only the citation is wrong.

### `microsoft/orca-math-word-problems-200k` — VERIFIED-AGREES
- License tag `mit` confirmed. Generation-method prose confirmed near-verbatim: *"All the
  answers in this dataset is generated using Azure GPT4-Turbo."*
- Held-at-NC-alongside-MetaMathQA reasoning: agrees (same OpenAI-ToS-tension shape, no
  blocking clause found).

### `TIGER-Lab/MathInstruct` — VERIFIED-AGREES
- Aggregate tag `mit` confirmed. Per-component licence table confirmed, including the exact
  claim: Camel-Math = **"Attribution-NonCommercial 4.0 International"** inside an otherwise
  MIT/Apache-2.0-mix table (GSM8K MIT, AQuA-RAT Apache-2.0, MATH MIT, TheoremQA MIT, NumGLUE
  Apache-2.0, MathQA Apache-2.0, GSM8K-RFT "Non listed"). This is a genuine, independently
  confirmed instance of the mirror-lies pattern (single blanket `mit` tag on the aggregate
  masking an NC component). "NC-if-whole" verdict per the strictest-input rule: agrees.

### `EleutherAI/proof-pile-2` — VERIFIED-AGREES
- `cardData.license` confirmed unset/blank. README confirms *"We do not alter the license of
  any of the underlying data"* and gives no specific AlgebraicStack licence. UNVERIFIED-pending
  status (recommend sourcing OpenWebMath directly instead): agrees.

### `nvidia/Nemotron-Math-Proofs-v1` — mirror tag confirmed, still correctly UNVERIFIED (no verdict rendered to contradict)
- Dataset exists; `cardData.license` = `cc-by-sa-4.0` confirmed, and the card's own "License/
  Terms of Use" section states the dataset "is governed by the Creative Commons
  Attribution-ShareAlike 4.0 International License" — this is in fact NVIDIA's own first-party
  card (NVIDIA is the publishing org on HF), so there may not be a separate "primary NVIDIA
  source" beyond this card to check further; I found no additional NVIDIA terms-of-use page
  narrowing or contradicting the CC-BY-SA-4.0 grant this session. The surveyor rendered no
  verdict (left as "plausible SHARE_ALIKE candidate for a future pass"), so nothing to contradict
  — but the UNVERIFIED label somewhat overstates the gap, since the card itself is a first-party
  NVIDIA statement, not a third-party mirror. Worth a lighter-touch re-classification next pass,
  not a correctness issue.

### `tasksource/proofwriter` — VERIFIED-AGREES
- `cardData.license` confirmed blank ("More Information needed" on the card).
- `github.com/allenai/proofwriter` confirmed 404 (surveyor's guessed URL was indeed wrong). My
  own search also failed to find a ProofWriter-specific GitHub repo with a clear licence file —
  the correct primary appears to be `allenai.org/data/proofwriter` (a data-download page, not a
  licensed code repo), which I did not find licence text on either. UNVERIFIED / held-refused:
  agrees, and remains a genuine follow-up task rather than a resolved finding.

### `maveriq/bigbenchhard` — VERIFIED-AGREES on verdict; one additional mirror-lies instance found
- Independently fetched `raw.githubusercontent.com/google/BIG-bench/main/LICENSE` myself: full
  Apache License 2.0 text confirmed — matches the surveyor's primary-source claim verbatim.
- New finding not in the survey: the `maveriq/bigbenchhard` HF card itself asserts, in its own
  prose, *"License of the github repo is MIT License"* — which is wrong; the actual
  `google/BIG-bench` repo LICENSE file is Apache-2.0, not MIT, per my direct fetch. This is a
  second, previously-uncaught mirror-lies instance (an uploader's incorrect claim about the
  upstream licence), running in the opposite direction from the usual pattern (mirror asserts a
  *specific wrong* licence rather than leaving the field blank). It does not change the verdict
  — both Apache-2.0 and MIT are PERMISSIVE_OK — but should be logged so a future pass doesn't
  cite the HF card's "MIT" claim as if it were checked.
- EVAL-ONLY flag (23-task hard subset, high B3 leakage risk): reasonable, agrees.

---

## REFUSED entries

### Multi-provider synthetic aggregates (`AMAImedia/NOESIS-*...`) — VERIFIED-AGREES
- Confirmed these datasets are real, not a hypothetical: `AMAImedia` has published
  `NOESIS-50K-reasoning-router-code-math-psych-opus47-deepseek4-qwen36-gemini31-r1-gpt54` and
  `NOESIS-1M-reasoning-router-code-math-psych-opus47-deepseek4-qwen36-gemini31-r1-gpt54` on HF
  (confirmed via the org's dataset listing). The filename-declared blend of five-plus
  proprietary/hosted model outputs (Opus, DeepSeek, Qwen, Gemini, GPT) is consistent with what
  the survey described. REFUSE-TERMS (no defensible verdict without a per-provider ToS review
  not attempted this session): agrees.

### Empty/`None` `cardData.license` + no primary fetched (`tasksource/proofwriter`) — VERIFIED-AGREES
- Covered above under the non-top-8 section; the REFUSE-by-default posture is correctly applied,
  not silently admitted.

### `zh-plus/tiny-imagenet`-style pattern — not applicable, correctly caveated
- Surveyor explicitly stated this pattern was "not hit directly" in this pass and only named the
  `aops_forum` flag as the closest analogue. Nothing to verify against a primary source since no
  specific candidate was claimed to match this pattern; the framing is honest about the
  distinction. No issue.

---

## Summary

Of 8 top-tier candidates + 8 notable-non-top-8 + 3 REFUSED entries (19 total items examined):

- **VERIFIED-AGREES** (verdict and evidentiary claims both hold up): 15 of 19 — candidates
  1, 2, 4, 5, 6, 7, 8 (top 8, all but #3), `AI-MO/NuminaMath-CoT`,
  `microsoft/orca-math-word-problems-200k`, `TIGER-Lab/MathInstruct`, `EleutherAI/proof-pile-2`,
  `tasksource/proofwriter`, `maveriq/bigbenchhard`, and both REFUSED entries verified.
- **CONTRADICTED (evidentiary claim only, verdict unaffected)**: 2 —
  - `open-web-math/open-web-math` (candidate #3): the claimed "blank `cardData.license` /
    mirror-lies gap" is false — the field is set to `odc-by` and agrees with the prose. The
    ATTRIBUTION verdict itself is correct on independent grounds.
  - `meta-math/MetaMathQA`: the GPT-3.5-rephrasing fact is true but is not present in the HF
    card prose as claimed — it comes from the paper/GitHub repo instead. The NC-hold verdict is
    unaffected.
- **UNVERIFIABLE / no verdict to contradict**: 1 — `nvidia/Nemotron-Math-Proofs-v1` (surveyor
  rendered no verdict; my check confirms the mirror tag and adds context that the card is
  first-party NVIDIA, not a third-party mirror, so "UNVERIFIED" may be an overstatement of the
  gap, but this is a framing note, not a contradiction).
- **New finding beyond the survey's scope**: `maveriq/bigbenchhard`'s own HF card asserts an
  incorrect "MIT" licence for the upstream `google/BIG-bench` repo (actual: Apache-2.0, which
  the survey correctly used from the true primary source) — a second mirror-lies instance the
  survey's own primary-source check happened to route around but didn't flag as a finding in its
  own right.

**Net assessment**: no dataset in the survey's top-8 or notable-non-top-8 lists needs its
verdict changed. Both contradictions found are evidentiary/citation errors (a claimed
API-field gap that isn't there; a fact correctly stated but mis-sourced to the wrong document)
rather than licence-verdict errors, and neither weakens the strictest-input accounting: the
composed set's binding constraint remains `camel-ai/math`'s NC (independently confirmed
`cc-by-nc-4.0`), and no candidate examined here carries an ND, no-redistribution,
no-training, or consent-revocable term.
