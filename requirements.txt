# Core dependencies - Updated 2026-01-19
# This file is provided for compatibility with tools that don't support pyproject.toml
# The primary dependency specification is in pyproject.toml (managed by uv)

# Core ML framework
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.26.0
pyyaml>=6.0

# API Server
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
python-multipart>=0.0.5
websockets>=10.0

# Documentation RAG system (optional - versions may need adjustment based on Python version)
# llama-index-core>=0.10.0
# llama-index-embeddings-huggingface>=0.5.0
# llama-index-vector-stores-faiss>=0.3.0
# faiss-cpu>=1.7.0

# NOTE: For exact pinned versions and full dependency resolution,
# use: uv sync --all-extras (from pyproject.toml)
