"""Visual-language surface: composite-frame construction for cross-faculty items.

`composite` is the deterministic layout engine and renderer W3r ships. `visual`
(`cogsyndelta.model.vl_jepa`) stays untouched here; W7v is the row that re-shapes it to
ingest what this package renders.
"""

from __future__ import annotations

__all__: list[str] = []
