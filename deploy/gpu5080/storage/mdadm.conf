# mdadm.conf
#
# Historical raid1 copy. Do NOT install over live /etc/mdadm/mdadm.conf.
# Live 2026-08-31T20:25Z gpu5080: RAID0 md127 name=gpu5080:bulk
# UUID=8d85a4cc:d1690f1e:5b147e0f:767d6d3e members sda1+sdb1, mount /bulk.
# ARRAY lines below are the old degraded raid1 UUID. Notes only.
#
# !NB! Run update-initramfs -u after updating this file.
# !NB! This will ensure that initramfs has an uptodate copy.
#
# Please refer to mdadm.conf(5) for information about this file.
#

# by default (built-in), scan all partitions (/proc/partitions) and all
# containers for MD superblocks. alternatively, specify devices to scan, using
# wildcards if desired.
#DEVICE partitions containers

# automatically tag new arrays as belonging to the local system
HOMEHOST <system>

# instruct the monitoring daemon where to send mail alerts
MAILADDR root

# definitions of existing MD arrays

# This configuration was auto-generated on Sat, 29 Aug 2026 17:33:46 -0400 by mkconf
DEVICE partitions
ARRAY /dev/md/bulk metadata=1.2 UUID=2fb1b150:fb96fedc:9f569b6b:ddfc2276
# BEGIN cabal bulk raid1
DEVICE partitions
ARRAY /dev/md/bulk metadata=1.2 name=gpu5080:bulk UUID=2fb1b150:fb96fedc:9f569b6b:ddfc2276
# END cabal bulk raid1
