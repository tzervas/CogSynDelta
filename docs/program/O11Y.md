# Observability — Datadog / Splunk analogues (already on homelab)

Do **not** add Tempo (AGPL). Traces are **Jaeger** (Apache-2.0).
Do not duplicate the UniFi security-net stack.

| Need | FOSS analogue | Frontend | Backend |
|---|---|---|---|
| Datadog APM (traces/spans) | Jaeger all-in-one | Jaeger UI (LAN `:16686`, security-net `.15`) | OTLP to collector `192.168.1.170` |
| Datadog dashboards/metrics | Grafana + VictoriaMetrics | https://grafana.vectorweight.com | `vm.vectorweight.com` → Grafana Explore |
| Logs + correlate | Grafana Loki | https://loki.vectorweight.com → Grafana Explore/Loki | unifi-loki |
| Splunk-like search | OpenSearch | internal (`unifi-opensearch`); Caddy commented until intentional | security-net `.21` |
| Status | Akula aggregator | https://status.vectorweight.com | `:9109` |

CSD lab **Steer/links** point at Grafana/Loki/VM/status. Autodev does
not get Grafana admin. Emit OTLP later behind `intern_parallel` flag.

Grafana 11.4 on this host was **dashboard-first** (UniFi stack). Explore
and Metrics/Logs drilldown are OSS flags, not extra images. Drop-in
`unifi-grafana.container.d/explore.conf`:

- `GF_EXPLORE_ENABLED=true`
- `GF_USERS_VIEWERS_CAN_EDIT=true` (Explore in the nav for Viewer; still not Admin)
- `GF_FEATURE_TOGGLES_ENABLE=exploreMetrics,exploreLogs,lokiQuerySplitting`

Hard-refresh / re-login at https://grafana.vectorweight.com. Left nav:
**Explore**, plus **Drilldown → Metrics / Logs**. Loki + VictoriaMetrics
datasources were already provisioned.

User **tyler** is **Editor** (Explore in the nav). Log out/in if the
left nav is still dashboard-only. Grafana OSS has no “power user”
role; Editor is the Explore equivalent. Admin remains `admin`.

## Mail (send-only MTA)

Grafana alerts go to `maintainers@vectorweight.com`. Homelab runs
**Postfix send-only** (`deploy/mail/`):

- Bind `127.0.0.1:25` + `172.30.0.1:25` only. Never `0.0.0.0`. Never
  WAN port-forward 25/587/465. `exim4` is masked.
- Clients: loopback + Grafana `172.30.0.13` only.
- Sender `grafana@vectorweight.com`; recipients `maintainers@` /
  `alerts@vectorweight.com` only. Not an open relay.
- MX/SPF stay Google Workspace. This box is not an MX.
- Grafana: `GF_SMTP_HOST=172.30.0.1:25`, contact point
  `maintainers-email`, default policy receiver that contact point.

If Gmail junks homelab-IP mail, switch `relayhost` to Workspace SMTP
relay (operator creds; not in the CSD vault). Do not open inbound SMTP
to fix deliverability.

## Project management (Forgejo gaps)

Forgejo 16 Projects API is 404 here. Use **milestones + labels + issues**
now (topics already on program repos). FOSS gap-fill (homelab, later):
**Plane** or **OpenProject** for roadmaps/Gantt; keep git on Forgejo.
Never put operator secrets in those apps — CSD bot account only.
